## 需求

1. 将 csust.edu.cn 及其所有子域名的所有页面和绝大多数功能。尽可能完整地抽象、内化并映射为可供智能体直接调用的 CLI，使 CLI 在功能覆盖、操作范围与实际使用效果上尽量贴近网页端。
2. 实现前应充分探索网站的页面、流程、入口与交互能力，形成必要的能力映射，再据此完成 CLI 设计与实现，并在完成后重新核查网站与 CLI 之间的覆盖情况，尽量避免功能缺失、路径遗漏或能力缩水。
3. 此项目不是无头浏览器，也不是爬虫客户端。
4. 爬虫仅用于开发阶段的探索。优先直接调用服务 API 或协议（如果有）。
   1. 仅提供HTML的服务使用 Adapter + Contract。
      1. Confidence Low 必须附带具体依据。

5. 正式业务命令使用语义化参数，地址解析和字段转换等由 Adapter 负责。
6. 统一同类业务对象的数据模型，但保留服务特有能力，不得为了统一而删减功能。
7. 避免依赖 DOM。
8. 保留完整写操作，明确成功判定。

## 功能完成的标志

1. 语义化命令
2. 统一结果模型
3. 结果验证

接口可调用不代表功能已实现。

## 技术栈

1. 全部迁移到 Go。