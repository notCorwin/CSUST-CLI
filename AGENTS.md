## 需求

1. 将 CSUST Web 服务体系的页面、服务、接口及绝大多数可用功能，尽可能完整地抽象、内化并映射为可供智能体直接调用的 CLI，使其在功能覆盖、操作范围和实际效果上尽量贴近网页端。
3. 本项目不是无头浏览器，也不是爬虫客户端。爬虫仅用于开发阶段的网站探索与能力发现。
4. 优先逆向并调用网站实际使用的服务 API，包括 Public、Internal 和 Undocumented API，并将其作为 CLI 的主要实现基础。
5. 网页侧能力与 CLI 业务层之间通过 Adapter 进行适配与隔离，使网站变化尽可能只需要维护对应 Adapter。
   1. 所有 `Confidence` 的判断必须附带具体依据。
6. 正式业务命令必须使用语义化参数，不直接暴露网页实现细节。
7. 对同类业务对象建立统一数据模型，同时完整保留各服务的特有能力，不得为了统一模型而牺牲功能。
8. 尽可能避免依赖 DOM；能够通过接口、协议或结构化数据完成的能力，不应依赖页面结构。
9. 完整保留可实现的写操作，并为每项写操作定义明确、可验证的成功判定。

## 功能完成的标志

1. 提供语义化命令。
2. 输出符合统一结果模型。
3. 对执行结果进行可靠验证。

**接口能够调用，不代表功能已经实现。**

## 技术栈

1. 全部迁移至 Go。