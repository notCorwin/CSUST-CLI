## 需求

1. 将 csust.edu.cn 及其所有子域名和所有 URL 路径面向用户提供的能力尽可能完整地抽象、内化并映射为可供智能体直接调用的 CLI，使 CLI 在功能覆盖、操作范围与实际使用效果上尽量贴近网页端。
2. 实现前应充分探索网站的页面、流程、入口与交互能力，形成必要的能力映射，再据此完成 CLI 设计与实现，并在完成后重新核查网站与 CLI 之间的覆盖情况，尽量避免功能缺失、路径遗漏或能力缩水。
3. CLI 应优先保持通用、稳定、结构化和可扩展，不局限于主要流程或显式入口；采用具有良好兼容性的设计，平衡性能与稳定性，使 CLI 能够自动适应大多数页面，从而不需要为每个页面写很多代码，也不用担心页面改版。
4. CLI 应该围绕服务设计，而不是网址。因此 CLI 参数不应该包括网址。

## 技术栈

1. 最新、稳定版本的 Python