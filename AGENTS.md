## 需求

1. 将 CSUST Web 服务体系的页面、服务、接口及绝大多数可用功能，尽可能完整地抽象、内化并映射为可供智能体直接调用的 CLI。
2. 优先收集、逆向实际使用的服务 API，包括 Public、Internal 和 Undocumented API，将其作为 CLI 的主要实现基础。
   1. 如果业务没有对应的 API 可用，那么网页侧能力与 CLI 业务层之间通过 Adapter 进行适配与隔离，使网站变化尽可能只需要维护对应 Adapter。

3. 正式业务命令必须使用语义化参数，不直接暴露网页实现细节。
4. 对同类业务对象建立统一数据模型，同时完整保留各服务的特有能力。
5. **避免依赖 DOM**：能够通过接口、协议或结构化数据完成的能力，不应依赖页面结构。
6. 为每项写操作定义明确、可验证的成功判定。

## 业务价值与探索优先级

网站探索应以**业务价值和任务需求**为准，目标是发现适合智能体调用的有效能力，而非穷举页面、接口或技术实现。覆盖率应按**业务能力覆盖率**衡量；技术复杂度、页面显著程度和可逆向程度都不代表业务价值。

优先级从高到低：

- `CORE`：核心业务能力，深入探索
- `ENABLER`：认证、权限、网关等基础能力，深入探索
- `UNIQUE`：学校特有能力，深入探索
- `COMMODITY`：可由通用产品替代的能力，只浅层记录
- `CONTENT`：纯内容或展示能力，通常不继续探索
- `UNKNOWN`：证据不足，继续有限探索直到能够分类

## 备注

1. 此 CLI 工具不是无头浏览器，也不是爬虫客户端。
2. 使用 playwright-cli 探索网页。
   1. Playwright 不进入代码。
